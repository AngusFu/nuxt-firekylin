{
  "name": "nuxt-firekylin",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "cross-env PORT=3333 nuxt",
    "build": "node ./build/",
    "start": "cross-env NODE_ENV=development npm run build && npm run dev",
    "generate": "cross-env NODE_ENV=production npm run build && nuxt generate",
    "clean": "rimraf dist/200.html && rimraf dist/**/*.map"
  },
  "dependencies": {
    "gitalk": "^1.5.0",
    "object-assign": "^4.1.1",
    "vue-fx": "^1.4.0",
    "vue-lazyload": "^1.3.1"
  },
  "devDependencies": {
    "cross-env": "^5.0.5",
    "fs-extra": "^4.0.1",
    "glob": "^7.1.2",
    "highlight.js": "^9.12.0",
    "indent": "^0.0.2",
    "js-yaml": "^3.9.1",
    "json-loader": "^0.5.7",
    "marked": "^0.3.6",
    "mkdirp": "^0.5.1",
    "nuxt": "^1.4.5",
    "rimraf": "^2.6.2",
    "yaml-front-matter": "^3.4.0",
    "yaml-loader": "^0.5.0"
  }
}
